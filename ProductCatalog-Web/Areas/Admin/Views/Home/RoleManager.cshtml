@using ProductCatolog_Core.VMs
@model ManagerUserRoleVM
@{
    ViewData["Title"] = "RoleManager";
}
<head><link rel="stylesheet" href="~/css/Admin/Home/roleManager.css" asp-append-version="true"/></head>

<div class="container mt-5">
    <h1 class="text-center mb-4">Manage Roles For User</h1>
    <hr />

    <form asp-action="RoleManager" method="post">
        <input type="hidden" asp-for="UserId" />

        <div class="my-3 text-center">
            <span class="fw-bold fs-4 text-dark">User Id: <span class="">@Model.UserId</span></span><br />
            <span class="fw-bold fs-4 text-dark">User Name: <span class="">@Model.UserName</span></span>
        </div>

        <hr />
        <table class="table table-responsive-sm table-hover table-striped">
            <thead class="thead-light">
                <tr>
                    <th>Role</th>
                    <th class="text-center">Assigned</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var role in Model.Roles)
                {
                    <tr>
                        <td>@role.RoleName</td>
                        <td class="text-center">
                            <input type="checkbox" asp-for="Roles[Model.Roles.IndexOf(role)].IsSelected" />
                            <input type="hidden" asp-for="Roles[Model.Roles.IndexOf(role)].RoleName" />
                        </td>
                    </tr>
                }
            </tbody>
        </table>
        <div class="d-flex justify-content-between mt-4">
            <button type="submit" class="btn btn-success btn-lg w-45">
                <i class="bi bi-save"></i> Save Changes
            </button>
            <a asp-action="GetCustomers" class="btn btn-outline-secondary btn-lg w-45">
                <i class="bi bi-arrow-left-circle"></i> Back to Customers
            </a>
        </div>
    </form>
</div>
